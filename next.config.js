/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // domains: ['ipfs.io'],
    // formats: ['image/png'],
  },
  swcMinify: false,
};

module.exports = nextConfig;
